# AI Adoption Leaderboard - Local Development Configuration
#
# For LOCAL DEVELOPMENT, use the Personal Access Token (PAT) mode.
# The OAuth flow requires a server (use for production on Vercel).

# ============================================
# Personal Access Token Mode (for local development)
# ============================================
#
# Option A (Recommended): Use your GitHub CLI token
#   If you have `gh` installed and authenticated, run:
#     gh auth token
#   and paste the output below. No need to create a separate token!
#
# Option B: Create a Personal Access Token
#   Create one at: https://github.com/settings/tokens
#   Requires 'repo' and 'read:user' scopes

GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GITHUB_REPOS=owner/repo1,owner/repo2

# Force PAT mode (bypasses OAuth detection)
AUTH_MODE_OVERRIDE=pat

# ============================================
# OAuth Mode (for testing OAuth locally - optional)
# ============================================
# If you want to test the OAuth flow locally:
# 1. Create an OAuth App at https://github.com/settings/developers
# 2. Set callback URL to: http://localhost:3000/api/auth/github/callback
# 3. Uncomment and fill in below:

# NEXT_PUBLIC_GITHUB_CLIENT_ID=your_client_id
# GITHUB_CLIENT_SECRET=your_client_secret
# AUTH_MODE_OVERRIDE=oauth